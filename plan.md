# Plan: prj-1910-task-mugotfeh

**Project:** `PRJ-952`  
**Task ID:** `task-mugotfeh`  
**Repo:** `prj-1910-task-mugotfeh`  

## Summary

Декомпозирую задачу TODO API на FastAPI + SQLite: структура проекта + модели, CRUD-эндпоинты, валидация + обработка ошибок, тесты, документация + финализация.

## Subtasks

### 1. Инициализировать структуру проекта и SQLAlchemy-модель Todo

- **ID:** `sub-1`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Создать pyproject.toml/requirements.txt с FastAPI, SQLAlchemy, uvicorn, pydantic, pytest. Реализовать database.py (engine, SessionLocal, Base, get_db dependency) и models.py (Todo: id, title, description, completed, created_at). Подключить SQLite-файл ./todos.db.
- **Test plan:** python -c 'from app.models import Todo; from app.database import Base, engine; Base.metadata.create_all(bind=engine)' завершается без ошибок; todos.db создаётся в корне.

### 2. Реализовать Pydantic-схемы и CRUD-эндпоинты

- **ID:** `sub-2`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Добавить schemas.py (TodoCreate, TodoUpdate, TodoOut). В main.py реализовать роутер /todos: POST /todos, GET /todos, GET /todos/{id}, PATCH /todos/{id}, DELETE /todos/{id}. Каждый эндпоинт использует Depends(get_db). Корректные status-коды: 201 для create, 404 если не найдено.
- **Test plan:** uvicorn app.main:app стартует без ошибок; POST /todos принимает JSON, возвращает 201 и объект с заполненным id; GET /todos возвращает список; DELETE возвращает 204.
- **Dependencies:** `sub-1`

### 3. Валидация, обработка ошибок и CORS

- **ID:** `sub-3`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Добавить кастомные HTTPException c единым форматом ошибок (detail + code). Глобальный exception-handler для ValidationError → 422. Middleware или CORSMiddleware с разрешёнными origins. Поле title — обязательное, max_length=200.
- **Test plan:** curl -X POST /todos с пустым title → 422; POST с title длиной 500 → 422; OPTIONS-запрос с Origin возвращает корректные CORS-заголовки.
- **Dependencies:** `sub-2`

### 4. Тесты pytest + httpx.AsyncClient

- **ID:** `sub-4`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Покрыть тестами все эндпоинты: create/list/get/update/delete, кейс 404, кейс невалидного title. Использовать отдельную in-memory SQLite (sqlite:///./test.db или :memory:) через фикстуру с пересозданием таблиц.
- **Test plan:** pytest -q — все тесты зелёные; coverage основных маршрутов ≥ 90%.
- **Dependencies:** `sub-3`

### 5. README, OpenAPI и финальная сборка

- **ID:** `sub-5`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Написать README.md с инструкцией запуска (uvicorn app.main:app --reload) и примерами curl. Проверить что /docs (Swagger UI) и /openapi.json доступны и валидны. Прогнать ruff/black + pytest перед сдачей.
- **Test plan:** /openapi.json содержит все 5 эндпоинтов под /todos; README содержит секции Install, Run, Examples; pytest -q зелёный.
- **Dependencies:** `sub-4`


---
*Generated by Hermes Orchestrator*
